name: 🎨 Linting

on:
  pull_request:
    branches: [ main, develop ]
  push:
    branches: 
      - main
      - develop
      - 'feat/**'
  workflow_dispatch:
  workflow_call:

jobs:
  eslint:
    name: ESLint & Prettier
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 📥 Install dependencies
        run: npm install

      - name: 🎨 Run ESLint
        run: npm run lint

      - name: ✨ Check formatting with Prettier
        run: npx prettier --check "src/**/*.{js,jsx,ts,tsx,vue,css,scss}"
        continue-on-error: true